export default class Environment {
    // region outerMethods

    AddObject(pos, id, args) {

    }
    // TODO Interaction with objects
    RemoveObject(id) {

    }
    DestroyObject(id, {xDir=0, yDir=0}) {

    }

    // endregion


    // region innerProcedures



    // endregion


    static init() {
        // initialize the map, after connection to the game
    }
}