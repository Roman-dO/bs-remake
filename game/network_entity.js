export default class NEntity {
    UpdatePos({xPos, yPos}) {
        // update the data to an actual values
        //  and updates view
    }
    ModifyPos({xForce, yForce}) {
        // sends request for server
    }

    constructor(diff, args) {

    }
}