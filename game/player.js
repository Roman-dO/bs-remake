export default class Player {
    // region outerInteractions

    UpdatePos({xPos, yPos}) {

    }
    UpdateDir({xMove, yMove}) {

    }
    UseSkill(dir, range) {

    }

    // endregion

    // region InnerProcedures

    static CreateDom() {
        
    }

    // endregion

    constructor(args) {

    }
}